# 尋章摘句 — 凍結 Schema 與模組介面（v1，MVP＝前 10 關）

本檔為四個平行工作線的共同契約，**任何人不得修改本檔與 data/fixtures/**。
發現契約有問題，回報主線程，不要自行變更。

## MVP 範圍

- 第 1 章共 10 關，全部 5×5，內容＝常用成語（四字），每關 3–5 個目標。
- 方向只有 `E`（→ 橫向，左到右）與 `S`（↓ 直向，上到下）。無斜向、無反向。
- 純前端無框架、無 build step、無外部依賴。測試用 `node --test`。

## data/phrases.json（語料庫，單一事實來源）

```json
{
  "id": "p0001",
  "text": "一心一意",
  "type": "成語",
  "level": "常用",
  "meaning": "形容心思專一，毫無雜念。",
  "insight": "多用於形容做事專注投入，如：他一心一意準備會考。",
  "textbook": false
}
```

- `id`：`p` + 四位數字，穩定遞增。
- `text`：MVP 一律四字成語，繁體，無標點。
- `type`：MVP 只有 `"成語"`（枚舉保留：名句/唐詩/宋詞/元曲）。
- `level`：MVP 只有 `"常用"`（枚舉保留：進階/挑戰）。
- `meaning`：本站自行撰寫的白話釋義（知識卡用），不夾帶外部辭典出處。`insight`：一句自撰用法或賞析（知識卡用）。
- `textbook`：是否為國中課本常見（布林，選填標記用）。

## data/levels.json（關卡，由 tools/generate-levels.mjs 預生成）

```json
{
  "levels": [
    {
      "id": 1,
      "chapter": 1,
      "size": 5,
      "directions": ["E", "S"],
      "targetDisplay": "text",
      "targets": [
        { "phraseId": "p0001", "start": [0, 0], "dir": "E" }
      ],
      "grid": [["一","心","一","意","風"], ["…共 5 列，每列 5 個單字"]]
    }
  ]
}
```

- 座標 `[row, col]`，0-based。`dir`: `E`=col 遞增、`S`=row 遞增。
- 目標句的路徑由 `start`+`dir`+字數推得，validate 必須重新掃描 grid 驗證一致。
- `targetDisplay`: MVP 一律 `"text"`（目標清單直接顯示句子）。
- `grid` 每格恰一個繁體中文字。

## 存檔（localStorage key `xzzj_save_v1`）

```json
{
  "v": 1,
  "levels": { "1": { "stars": 3, "found": ["p0001"] } },
  "ink": 4,
  "collection": ["p0001"],
  "quizStats": { "answered": 10, "correct": 7 }
}
```

星等規則：通關=1★；全程未用「直接揭示」=2★；完全未用任何提示=3★。

## 模組介面（js/，皆為瀏覽器 ES module；hints/learnquiz/collection 零 DOM 零 fetch，可被 node --test 直接 import）

### js/hints.js

```js
export const COSTS = { circle: 1, flash: 3, reveal: 5 };
export const EARN  = { choice: 1, fill: 2 };
export function createHintEngine(savedInk = 0) {
  return {
    getInk(),            // → number
    earn(kind),          // kind ∈ 'choice'|'fill'；只有 game 在「答對」時呼叫；回傳新餘額
    canSpend(tier),      // tier ∈ 'circle'|'flash'|'reveal' → boolean
    spend(tier),         // 扣款成功回 true；不足回 false 且不扣
    serialize()          // → number（存檔用餘額）
  };
}
```

不變式（tests 必鎖）：墨水只能由 `earn` 增加；`spend` 不足時不得變負；初始餘額=存檔值。

### js/learnquiz.js

```js
export function buildQuestions(phrases, targetIds, count) { /* → Question[] */ }
// Question = {
//   type: 'choice' | 'fill',
//   prompt: string,          // choice: 白話釋義；fill: 挖空句如「一［　］一意」
//   options?: string[4],     // choice 專用，含正解共 4 個成語，順序隨機
//   answer: string,          // choice: 正解成語全文；fill: 缺的那個字
//   phraseId: string
// }
```

- 題源優先從 `targetIds`（本關目標）抽，不足才從全庫補（「題目即提示」設計）。
- choice 的干擾選項從同 type 語料抽，不得與正解同義。
- fill 挖空位置隨機，一次只挖一字。
- 必須可傳入 seed 函式或以 Math.random 注入方式測試（介面：第 4 個選填參數 `rng = Math.random`）。

### js/collection.js

```js
export function createCollection(savedIds = []) {
  return {
    add(phraseId),   // 冪等；只有 game 在「關卡中真實找到」時呼叫
    has(phraseId),   // → boolean
    list(),          // → string[]（依加入順序）
    serialize()      // → string[]
  };
}
```

### 前端整合點（js/game.js 持有）

- 玩家答對學習題 → `hintEngine.earn(q.type === 'choice' ? 'choice' : 'fill')`
- 玩家在格中找到目標 → `collection.add(phraseId)` + 彈知識卡（meaning/insight）
- 提示三層：circle=圈出該句首字位置；flash=整句路徑高亮 2 秒；reveal=直接標記為已找到（該關星等封頂 1★）。

## 檔案所有權（不得越界寫檔）

| 工作線 | 只准寫 |
|---|---|
| A 語料 | `data/phrases.json`、`data/raw/**` |
| B 工具 | `tools/**` |
| C 前端 | `index.html`、`css/**`、`js/grid.js`、`js/game.js`、`js/progress.js`、`js/app.js` |
| D 學習模組 | `js/hints.js`、`js/learnquiz.js`、`js/collection.js`、`tests/**` |

共用凍結檔：`docs/SCHEMA.md`、`data/fixtures/**`。`data/levels.json` 由主線程事後執行產生器產出。

---

# Schema v2 增補（線索化＋填字模式）——本節同樣凍結

v2 兩大變更：**目標一律以創意線索呈現（不直接顯示成語）**；**新增 cross（填字）版型**。

## phrases.json v2：每條加 `clues`

```json
"clues": [
  { "style": "釋義", "text": "比喻拘泥成規不知變通。" },
  { "style": "急轉彎", "text": "有人在船上掉了劍，卻拿小刀在船邊做記號，他在幹嘛？" },
  { "style": "典故", "text": "楚國人的劍掉進江裡，他卻在船舷刻記號，打算靠岸再撈。" }
]
```

- `style` 枚舉：`釋義`（白話解釋猜謎）／`急轉彎`（腦筋急轉彎）／`典故`（典故情節描述，不點名成語）／`諧音`（諧音梗、雙關笑話）／`情境`（生活化情境短劇，台灣接地氣哏）。
- 每條至少 3 則：**必含 1 則「釋義」**（前期關卡用、最好猜）＋至少 2 則創意型（急轉彎/典故/諧音/情境擇二）。
- 硬規則：線索文字**不得包含完整成語**，也**不得包含成語中任兩個相鄰字連用**（如「守株待兔」的線索不得出現「守株」「株待」「待兔」字樣；單字出現允許；諧音同音異字允許）。
- 沒有典故的成語不硬編典故——換成急轉彈/諧音/情境即可，禁止捏造假典故。

## levels.json v2

```json
{
  "id": 7, "chapter": 1, "size": 7,
  "layout": "cross",              // "full"=尋句（灌滿干擾字）；"cross"=填字十字交叉
  "directions": ["E", "S"],
  "targetDisplay": "clue",        // v2 一律 "clue"
  "targets": [
    { "phraseId": "p0018", "start": [0,2], "dir": "S", "clueIndex": 1 }
  ],
  "grid": [[null, "…", null]],   // cross：目標路徑格存答案字元，其餘 null；full：同 v1 全字元
  "revealed": [[0,2], [2,4]]      // 僅 cross：預先顯示的格子（＝交叉點），其餘目標格畫空框
}
```

- `clueIndex` 指向該 phrase 的 `clues[clueIndex]`，產生器選定；validate 必驗 index 存在。
- 第一章配置：關 1–5 `full`（5×5；目標數 3,3,4,4,4）、關 6–7 `cross`（7×7、4 目標）、關 8–10 `cross`(8×8、5 目標)。
- 線索難度曲線：關 1–2 一律用「釋義」；關 3 起產生器輪換創意型（急轉彎/典故/諧音/情境），同一關內 style 盡量不重複。
- cross 擺盤規則：每個詞至少與另一個詞交叉一次（共用同一字），全部詞構成一個連通network；`revealed` = 恰好所有交叉格；無干擾字。
- validate 增驗：cross 的非 null 格都屬於某目標路徑；交叉格兩詞字元一致；revealed 恰等於交叉格集合；線索硬規則（不含完整成語、不含相鄰兩字連用）。

## 前端互動 v2

- 目標清單 chips：顯示 style 小標籤＋線索文字（不顯示成語）；找到/填對後翻面成成語＋劃線。
- `full` 關卡互動不變（拖選）。
- `cross` 關卡：null 格不渲染；revealed 格顯示字；其餘目標格畫空框。點任一目標格或其 chip → 選中該詞（路徑高亮＋線索顯示）→ 輸入框輸入四字成語 → 正確＝整詞填入（交叉連動）、collection.add、知識卡；錯誤＝紅閃。
- 提示三層在 cross 的對映：圈首字＝填顯該詞首格；閃現＝該詞全字暫顯 2 秒；揭示＝直接填入（仍 1★、不入圖鑑）。
- 存檔格式不變。

---

# Schema v3 增補（50 關課程表：諺語俗語＋提示限次＋時間限制）——本節同樣凍結

遊戲設計原則（八角框架）：難度三軸並進——「盤面複雜度」（尺寸/目標數/交叉密度）、
「資源壓力」（提示限次）、「時間壓力」（倒數計時）。每章只調升其中兩軸，避免挫敗感疊加；
線索風格從好猜的釋義逐章轉向創意型，最終章幾乎全創意線索。計時在教育性彈窗
（知識卡/學習題/結算）開啟時**必須暫停**——看解說不該被扣時間。

## phrases.json v3

- `type` 枚舉擴充：`成語`｜`諺語`｜`俗語`（保留：名句/唐詩/宋詞/元曲）。
- `level` 枚舉擴充：`常用`｜`進階`。
- 諺語/俗語：`text` 4–9 字、無標點；必須是廣為流傳的真實諺語/俗語，禁自創；白話釋義一律由本站重新撰寫；台語俗諺一律
  改用華語通行寫法或不收。clues 規則與 v2 相同（≥3、必含釋義、硬規則同）。

## levels.json v3 新欄位

```json
{
  "id": 11, "chapter": 2, "chapterTitle": "廟口智慧",
  "timeLimit": 300,     // 秒；null = 無時限
  "hintCap": 3,         // 本關提示總次數上限（三種提示合計）；null = 不限
  ...其餘同 v2
}
```

## 50 關課程表（產生器照表施工；validate 逐項核對）

| 章 | 關 | 版型 | 內容池 | 目標數 | timeLimit | hintCap | 線索曲線 |
|---|---|---|---|---|---|---|---|
| 1 初窺門徑 | 1–5 | full 5×5 | 成語·常用 | 3,3,4,4,4 | null | null | 關1–2釋義；3起輪創意 |
| 1 | 6–7 | cross 7×7 | 成語·常用 | 4 | null | null | 同上 |
| 1 | 8–10 | cross 8×8 | 成語·常用 | 5 | null | null | 同上 |
| 2 廟口智慧 | 11–14 | full 7×7 | 諺語俗語 | 4,4,5,5 | 300 | 3 | 釋義/情境各半 |
| 2 | 15–17 | cross 9×9 | 諺語俗語 | 4 | 300 | 3 | 同上 |
| 2 | 18–20 | cross 10×10 | 諺語俗語 | 5 | 330 | 3 | 同上 |
| 3 成語風雲 | 21–24 | full 8×8 | 成語·進階 | 5,5,6,6 | 270 | 2 | 創意優先 |
| 3 | 25–27 | cross 9×9 | 成語·進階 | 5 | 270 | 2 | 創意優先 |
| 3 | 28–30 | cross 10×10 | 成語·進階 | 6 | 300 | 2 | 創意優先 |
| 4 龍虎混戰 | 31–34 | full 9×9 | 成語+諺語俗語混 | 6,6,7,7 | 270 | 2 | 創意優先 |
| 4 | 35–37 | cross 10×10 | 混合 | 6 | 270 | 2 | 創意優先 |
| 4 | 38–40 | cross 11×11 | 混合 | 6 | 300 | 1 | 創意優先 |
| 5 宗師試煉 | 41–44 | full 10×10 | 全庫混合 | 7,7,8,8 | 240 | 1 | 幾乎全創意 |
| 5 | 45–47 | cross 11×11 | 全庫混合 | 7 | 270 | 1 | 幾乎全創意 |
| 5 | 48–49 | cross 12×12 | 全庫混合 | 7 | 270 | 1 | 幾乎全創意 |
| 5 | 50 宗師之戰 | cross 12×12 | 全庫混合 | 8 | 300 | 1 | 全創意 |

- 「創意優先」＝該成語有急轉彎/典故/諧音/情境就用，同關 style 盡量不重複；「幾乎全創意」＝除非缺料否則不用釋義。
- 內容池：`成語·常用`=type成語&level常用；`成語·進階`=type成語&level進階；`諺語俗語`=type∈{諺語,俗語}；混合=兩池各半（無法整除時成語多一）。
- full 版型自 v3 起支援 4–9 字變長目標（諺語）；cross 交叉規則不變（E 交 S、恰一交叉格、連通）。
- 跨關仍優先抽未用過的條目；語料量須足以讓 50 關「不重複優先」大致成立（成語 120＋諺語俗語 80）。

## 計時與提示限次（前端行為）

- 倒數顯示於關卡標題列；剩 20% 時間變紅脈動。任何 modal（知識卡/學習題/通關/超時）開啟時暫停。
- 超時 → 「時辰已到」彈窗（僅「重新挑戰」「回地圖」兩鈕）→ 重新挑戰＝本關 found 清空
  （含 levelSave.found 覆寫為空）、盤面重置、計時重置；已入圖鑑的收藏**不回收**（收藏=真實學過）。
- hintCap：關卡內三種提示合計次數上限；剩餘次數顯示於提示列（「提示 2/3」）；用罄→三鈕停用
  （賺墨水鈕不受限，墨水仍可留到別關用）。null＝不顯示限次。
- 星等規則不變；超時重來後星等從乾淨狀態計。
- 存檔格式不變（計時/限次都是關卡內暫態）。

## v3 檔案所有權

| 線 | 只准寫 |
|---|---|
| A3 語料 | `data/phrases.json`（追加 p0061 起）、`data/raw/**` |
| B3 工具 | `tools/**` |
| C3 前端 | `index.html`、`css/**`、`js/{app,grid,game,progress}.js` |

hints/learnquiz/collection/tests 本輪不動（提示限次屬 game.js 關卡邏輯，不進 hints.js 純模組）。

---

# Schema v4 增補（第 51–100 關：五大文學魔王章，本節同樣凍結）

主題：盤面「區域變大」——從 13×13 一路推到終章 20×20；內容改用漢賦、古詩十九首、
樂府詩、新樂府詩、唐詩、宋詞、元曲、章回小說的真實名句。這批內容是**真實文學引文**，
不是我們自己造的成語/諺語，字句必須逐字核實，不可自創或憑印象亂拼。

## phrases.json v4：新增 8 種 type

- `type` 枚舉再擴充：`漢賦`｜`古詩十九首`｜`樂府詩`｜`新樂府詩`｜`唐詩`｜`宋詞`｜`元曲`｜`章回小說`
  （v1–v3 的 `成語`｜`諺語`｜`俗語` 不變）。
- 這 8 種文學類型**額外必填** `author`（作者；佚名一律寫「佚名」）與 `dynasty`（朝代，如
  「盛唐」「北宋」「元」）——這是史實歸屬，不算 v3 禁止的「外部辭典抄襲」，允許保留。
  `成語`｜`諺語`｜`俗語` 三型維持 v3 規則：不得帶 `source`／`author`／`origin_work`／`dynasty`。
- `level` 沿用既有枚舉，這 8 種文學類型一律填 `進階`。
- `text`：逐字覆核的真實原文片段（單句或半聯，不含標點），長度 4–14 字（多數落在 5–9 字，
  只有少數對句／長聯型內容用到 10–14 字，供終章大盤面使用）。**禁止竄改字句、禁止拼接
  不同出處的句子冒充一句、禁止自創「疑似古文」**。查無實據的句子寧可不收。
- `meaning`／`insight`：沿用 v3 鐵律——站內原創白話撰寫（不可整段抄語譯本或维基），
  `meaning` 是老師口吻講清楚這句在說什麼，`insight` 是延伸一層的今日提醒／生活連結。
- `clues`：規則不變（≥3 則、必含「釋義」、style 屬既有五種枚舉、不含完整原句、不含原句
  任兩相鄰字連用、無簡體字、無捏造典故）。文學類型的「典故」線索可用作者生平/寫作背景，
  但背景敘述必須查有實據，不可杜撰情節；「急轉彎」可比照使用者原意用作者其他作品或
  生命歷程給一兩字線索。

## levels.json v4：第 51–100 關課程表（五章，13×13 → 20×20）

| 章 | 關 | 章名 | 版型 | 尺寸 | 內容池 | 目標數 | timeLimit | hintCap | 線索曲線 |
|---|---|---|---|---|---|---|---|---|---|
| 6 漢賦遺韻 | 51–54 | full | 13×13 | 漢賦十九首 | 8,8,9,9 | 320 | 2 | 創意優先 |
| 6 | 55–57 | cross | 14×14 | 漢賦十九首 | 9 | 300 | 2 | 創意優先 |
| 6 | 58–60 | cross | 14×14 | 漢賦十九首 | 10 | 300 | 2 | 創意優先 |
| 7 樂府新聲 | 61–64 | full | 14×14 | 樂府新樂府 | 9,9,10,10 | 300 | 2 | 創意優先 |
| 7 | 65–67 | cross | 15×15 | 樂府新樂府 | 10 | 290 | 1 | 創意優先 |
| 7 | 68–70 | cross | 15×15 | 樂府新樂府 | 11 | 290 | 1 | 創意優先 |
| 8 盛唐詩陣 | 71–74 | full | 15×15 | 唐詩 | 10,10,11,11 | 290 | 1 | 幾乎全創意 |
| 8 | 75–77 | cross | 16×16 | 唐詩 | 11 | 280 | 1 | 幾乎全創意 |
| 8 | 78–80 | cross | 16×16 | 唐詩 | 12 | 280 | 1 | 幾乎全創意 |
| 9 宋詞元曲 | 81–84 | full | 16×16 | 宋詞元曲 | 11,11,12,12 | 280 | 1 | 幾乎全創意 |
| 9 | 85–87 | cross | 17×17 | 宋詞元曲 | 12 | 270 | 1 | 幾乎全創意 |
| 9 | 88–90 | cross | 17×17 | 宋詞元曲 | 13 | 270 | 1 | 幾乎全創意 |
| 10 回目千秋 | 91–94 | full | 18×18 | 章回小說 | 12,12,13,13 | 270 | 1 | 全創意 |
| 10 | 95–97 | cross | 18×18 | 章回小說 | 13 | 260 | 1 | 全創意 |
| 10 | 98–99 | cross | 19×19 | 章回小說 | 14 | 260 | 1 | 全創意 |
| 10 | 100 終極魔王 | cross | 20×20 | 文學全庫 | 15 | 420 | 0 | 全創意 |

- 內容池：`漢賦十九首`＝type∈{漢賦,古詩十九首}；`樂府新樂府`＝type∈{樂府詩,新樂府詩}；
  `唐詩`＝type=唐詩；`宋詞元曲`＝type∈{宋詞,元曲}；`章回小說`＝type=章回小說；
  `文學全庫`＝以上 8 種文學 type 全部（不含成語/諺語/俗語，第 100 關是純文學總會考）。
  這些池皆非 v3 的「混合」池，不套用成語/諺俗各半配額邏輯。
- 第 100 關 hintCap=0：從進關那一刻起三種提示鈕全部停用（「賺墨水」不受影響、墨水可留到
  日後——但這關就是不給提示的真魔王），timeLimit 拉長到 420 秒補償。
- 其餘規則（碰撞掃描、cross 交叉/連通、跨關優先不重複、計時暫停、星等）與 v3 完全相同。

## v4 檔案所有權

| 線 | 只准寫 |
|---|---|
| A4×5 語料 | `data/raw/lit-*.json`（新檔，不得動 v1–v3 已產出的 phrases.json 本體） |

`tools/curriculum.mjs`／`tools/generate-levels.mjs`／`tools/validate.mjs`／`docs/SCHEMA.md` 本節
由主線程統一改（課程表是單一事實來源，多線平行改會衝突）；5 條語料線只新增各自的
`data/raw/lit-*.json`，主線程收斂後統一編號合併進 `data/phrases.json`。
